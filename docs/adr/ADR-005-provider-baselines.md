# ADR-005 — V1 provider baselines

**Status:** Accepted

TTS: ElevenLabs + Typecast + explicitly selected Kokoro Local. The Goal 09
owner amendment authorizes the local provider without changing existing project
selections or enabling silent fallback. Planning LLM: Gemini API with
`gemini-3.5-flash` baseline and `gemini-3.6-flash` benchmark candidate. Visual
generation: Google Flow. Flow video source audio is muted; narration is master
audio.

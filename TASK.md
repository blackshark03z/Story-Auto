# STORY_AUTO_V1_FULL_IMAGE_ONLY_CLOSE

## Current product scope

- Release-supported: `full_image`.
- Deferred and provider-blocked: `hybrid_hook`, `full_video_ai`.
- Preserved historical/local candidate: `ambient_story`; not release-supported.

The UI keeps Intro Video + Images and Full Video visible as disabled Coming
soon choices. Backend creation and production entry points reject every mode
except Full Image before provider calls. Existing projects and deferred source
remain readable and are not migrated or deleted.

## Resume boundary

Do not resume Flow UI reference-attachment experiments through gallery order,
newest/first tile, filenames, wrapper aliases, perceptual gallery scans, direct
file reassignment, or private endpoint replay. A future Tech Lead must select
and authorize a different provider strategy before reopening Hybrid/Video.

Preserve ambiguous provider evidence, the canonical Flow browser profile/auth
state, and protected runtime projects. Never retry an unresolved provider
attempt merely because the local process stopped.
